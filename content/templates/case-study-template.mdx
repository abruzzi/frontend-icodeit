---
title: "Case Study Name"
summary: "One-line pitch: what the reader will learn and for which product shape."
kind: "case-study"
status: "published | planning | coming-next | in-progress"
scenario: "feed-list | board-app | chat-app | typeahead | video-page | whiteboard"
tags: ["performance", "testing", "a11y"]
patternRefs: []
quickTake:
  - "The main technical idea in one sentence (e.g. normalized cache + cursor + virtualization)."
  - "The second idea: hardest constraint (latency, a11y, realtime, consistency)."
productionNotes:
  - "What you would measure or alert on in production."
  - "Non-negotiable rollout or accessibility guardrail."
testingRubricId: "dynamic-list-core | realtime-collaboration-core | typeahead-core"
---

<!--
  **Path:** `content/case-studies/<slug>/index.mdx` (one folder per case study).
  Optional co-located `snippets/` and `assets/`; register client demos from
  `components/case-studies/<slug>/` in `components/mdx/mdx-components.tsx`.

  **Learning guide** (not an interview timer): explain the system so a motivated engineer
  can implement or teach it. Use CCDAO as sections only where they help readability.

  Suggested flow for a *full* article:
  - Opening: what you are building and who it is for
  - Minimal slice / “get this path working first”
  - Product & engineering constraints (requirements without mock-interview framing)
  - Why it is hard (failure modes that drive design)
  - UI regions and state ownership
  - **Data modeling** (entities, client shape, ordering, idempotency, concurrency) — go deep
  - **API design** (reads, writes, errors, optional realtime, auth) — concrete shapes
  - Performance, reliability, accessibility
  - Trade-offs (short table or prose, no “pick 2–4 in session”)
  - Common bugs / debugging lens
  - <TestingRubric rubricId="..." />
  - Further reading

  Use `status: planning` (or `coming-next`) plus a single body sentence until the topic is ready.

  Optional MDX: <Callout />, <CcdaoFlowDiagram />, <PaginationMotionDemo />, <TradeoffMatrix />
-->

## Opening

[What the reader will build and learn.]

## Minimal slice

[First end-to-end path that must work.]

## Data modeling

[Deep dive: entities, client cache, ordering, operations, concurrency.]

## API design

[Reads, writes, idempotency, errors, optional realtime.]

## Testing checklist

<TestingRubric rubricId="dynamic-list-core" />
